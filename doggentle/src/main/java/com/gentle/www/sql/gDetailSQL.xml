<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
			"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="goods">
	<!-- 물품이름별 카테고리 검색 질의명령 -->
	<select id="option" resultType="gDetailVO" parameterType="gDetailVO">
		SELECT
		    gno, gname, g.cano, c.cano, caname
		FROM
		    goods g, category c
		WHERE
		    g.cano = c.cano
		    AND issell = 'Y'
		    AND gname in (select gname from goods where SUBSTR(GNAME, 1, 2) like #{gname})
	</select>
	
	<!-- 물품리스트 조회 질의명령 -->
	<select id="singerInfo" resultType="songVO" parameterType="int" >
		SELECT
		   gno, gname, cano
		FROM
		    goods
		WHERE
		    gno = #{gno}
	</select>
	</mapper>